import { ConfiguratorContext } from "../contracts";

export function applyConfirmState(_ctx: ConfiguratorContext): bool {
  // Placeholder finalization. Later we can commit ledgers or emit events.
  return true;
}
